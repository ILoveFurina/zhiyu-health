import { PageContainer } from '@ant-design/pro-components';

export default function WorkbenchPage() {
  return (
    <PageContainer title="工作台">
      <p>医生工作台建设中：排班、接诊队列、处方审核等能力将在后续迭代接入。</p>
    </PageContainer>
  );
}
