/* 对话记录抽屉样式（票 27 决策 6/9）；从 index.acss 拆出以保持单文件职责与行数约束 */

/* ===== 右上角对话记录入口（决策 9：自绘按钮，不用平台 option menu） ===== */
.option-menu-btn {
  position: absolute;
  top: var(--zy-space-sm);
  right: var(--zy-space-md);
  z-index: 20;
  width: 56rpx;
  height: 56rpx;
  border-radius: 50%;
  background: #fff;
  box-shadow: 0 2rpx 6rpx rgba(0, 0, 0, 0.08);
  color: var(--zy-color-primary);
  font-size: 36rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* ===== 对话记录抽屉 ===== */
.drawer-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.4);
  z-index: 60;
}

.drawer {
  position: absolute;
  top: 0;
  right: 0;
  bottom: 0;
  width: 78%;
  background: #f7f9f8;
  display: flex;
  flex-direction: column;
}

.drawer-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: var(--zy-space-md) 28rpx;
  background: #fff;
  border-bottom: 1rpx solid #eee;
}

.drawer-title {
  font-size: 30rpx;
  font-weight: 600;
  color: var(--zy-text-title);
}

.drawer-close {
  font-size: 40rpx;
  color: var(--zy-text-weak);
  padding: 0 8rpx;
}

.new-chat {
  display: flex;
  align-items: center;
  gap: 14rpx;
  margin: 20rpx var(--zy-space-md) var(--zy-space-xs);
  padding: 22rpx var(--zy-space-md);
  background: #fff;
  border-radius: var(--zy-radius-card);
  border: 1rpx solid var(--zy-color-primary);
  color: var(--zy-color-primary);
}

.new-chat-ico {
  width: 44rpx;
  height: 44rpx;
  border-radius: 12rpx;
  background: var(--zy-color-primary);
  color: #fff;
  font-size: 32rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  flex: 0 0 auto;
}

.new-chat-text {
  font-size: 28rpx;
  font-weight: 600;
}

.drawer-list {
  flex: 1;
  padding: var(--zy-space-xs) var(--zy-space-md);
  box-sizing: border-box;
}

.drawer-state {
  text-align: center;
  color: var(--zy-text-weak);
  font-size: 26rpx;
  padding: 80rpx 0;
}

.conv-item {
  background: #fff;
  border-radius: var(--zy-radius-card);
  padding: var(--zy-space-md) 26rpx;
  margin: var(--zy-space-sm) 0;
  box-shadow: 0 2rpx 6rpx rgba(0, 0, 0, 0.05);
}

.conv-item-current {
  border: 1rpx solid var(--zy-color-primary);
}

.conv-title {
  font-size: 28rpx;
  color: var(--zy-text-title);
  overflow: hidden;
  white-space: nowrap;
  text-overflow: ellipsis;
}

.conv-meta {
  margin-top: 12rpx;
  display: flex;
  align-items: center;
  gap: var(--zy-space-sm);
}

.conv-time {
  font-size: 22rpx;
  color: var(--zy-text-weak);
}

.conv-active {
  font-size: 22rpx;
  color: #e64545;
}

.drawer-tip {
  text-align: center;
  font-size: 20rpx;
  color: var(--zy-text-weak);
  padding: var(--zy-space-sm) 0 28rpx;
}
