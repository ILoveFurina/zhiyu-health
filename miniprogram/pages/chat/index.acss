@import "./report.acss";
@import "./redflag.acss";
@import "./drawer.acss";
@import "./feature-bubbles.acss";
@import "./health-profile.acss";

/* 聊天页（演示主画面），视觉对齐 docs/specs/0002-ui-conventions.md */
.page {
  display: flex;
  flex-direction: column;
  height: 100vh;
}

.thread {
  flex: 1;
  padding: var(--zy-space-md);
  box-sizing: border-box;
}

.thread-tail {
  height: 2rpx;
}

/* ===== 空状态 ===== */
.empty {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding-top: 120rpx;
}

.appointments-link {
  align-self: flex-end;
  margin-bottom: 20rpx;
  color: var(--zy-color-primary);
  font-size: 24rpx;
}

.mascot {
  width: 176rpx;
  height: 176rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, var(--zy-color-primary-soft), var(--zy-color-primary-soft));
  color: var(--zy-color-primary);
  font-size: 76rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.greet {
  margin-top: 28rpx;
  font-size: 34rpx;
  font-weight: 600;
}

.greet .brand {
  color: var(--zy-color-primary);
}

.greet-sub {
  margin-top: 10rpx;
  font-size: 24rpx;
  color: var(--zy-text-weak);
}

.topics {
  width: 100%;
  margin-top: 40rpx;
  display: flex;
  flex-direction: column;
  gap: var(--zy-space-sm);
}

.topic {
  background: #fff;
  border-radius: var(--zy-radius-card);
  padding: var(--zy-space-md) 26rpx;
  display: flex;
  align-items: center;
  gap: var(--zy-space-sm);
  box-shadow: var(--zy-shadow-card);
  transition: transform var(--zy-motion-fast) var(--zy-ease-out);
}

.topic:active {
  transform: scale(0.98);
}

.topic .hash {
  width: 40rpx;
  height: 40rpx;
  border-radius: var(--zy-radius-sm);
  background: var(--zy-color-primary);
  color: #fff;
  font-size: 24rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  flex: 0 0 auto;
}

.topic .topic-text {
  font-size: 26rpx;
  color: var(--zy-text-title);
}

.topic .arrow {
  margin-left: auto;
  color: var(--zy-text-weak);
  font-size: 32rpx;
}

/* 空态 AI挂号助手主卡（票 49）：.empty 为居中 flex，主卡需撑满宽度 */
.reg-entry-wrap {
  width: 100%;
  margin-top: var(--zy-space-lg);
}

/* ===== 气泡 ===== */
.bubble {
  max-width: 78%;
  padding: 20rpx var(--zy-space-md);
  border-radius: var(--zy-radius-card);
  font-size: 28rpx;
  line-height: 1.55;
  margin: var(--zy-space-sm) 0;
  word-break: break-all;
}

/* 票 66：自己发言气泡按手册例外收编为主色底 + 白字 */
.bubble.user {
  margin-left: auto;
  background: var(--zy-color-primary);
  color: #fff;
  border-bottom-right-radius: var(--zy-radius-sm);
  width: fit-content;
}

/* image kind 消息：用户上传的拍照原图缩略图（ADR-0023 回拉 MinIO 原图） */
.image-bubble {
  display: flex;
  align-items: center;
}

.image-msg {
  width: 240rpx;
  height: 240rpx;
  border-radius: var(--zy-radius-sm);
  display: block;
}

.image-msg-label {
  display: flex;
  align-items: center;
  gap: 8rpx;
  font-size: 26rpx;
}

.bubble.ai {
  margin-right: auto;
  background: #fff;
  color: var(--zy-text-title);
  border-bottom-left-radius: var(--zy-radius-sm);
  box-shadow: var(--zy-shadow-card);
  width: fit-content;
}

/* 票 50：号源查询失败卡「重新查询」按钮（发起新一轮对话，携带 retry_standard_department_id） */
.depslots-retry {
  margin-top: 16rpx;
  padding: 10rpx 36rpx;
  border-radius: 999rpx;
  background: var(--zy-color-primary);
  color: #fff;
  font-size: 26rpx;
  width: fit-content;
  transition: transform var(--zy-motion-fast) var(--zy-ease-out);
}

.depslots-retry:active {
  transform: scale(0.98);
}

/* 票 44：情绪反馈气泡配色--calm 白泡（默认）、anxious 暖橙、fearful 暖红 */
.bubble.ai-anxious {
  background: var(--zy-color-warning-soft);
  border-left: 4rpx solid var(--zy-color-warning);
}

.bubble.ai-fearful {
  background: var(--zy-color-danger-soft);
  border-left: 4rpx solid var(--zy-color-danger);
}

/* 安抚语：附 AI 气泡底部 disclaimer 上方，与回复共用 disclaimer，不单独标注 */
.soothing {
  margin-top: 12rpx;
  padding-top: 12rpx;
  border-top: 1rpx solid rgba(0, 0, 0, 0.06);
  font-size: 24rpx;
  line-height: 1.55;
  color: var(--zy-text-weak);
}

.cursor {
  color: var(--zy-color-primary);
}

/* ===== 票 52：AI 气泡 Markdown 块样式 ===== */
.md-p, .md-li, .md-h1, .md-h2, .md-h3 {
  margin-top: 12rpx;
}

.md-p:first-child, .md-li:first-child,
.md-h1:first-child, .md-h2:first-child, .md-h3:first-child {
  margin-top: 0;
}

.md-h1 {
  font-size: 34rpx;
  font-weight: 600;
}

.md-h2 {
  font-size: 31rpx;
  font-weight: 600;
}

.md-h3 {
  font-size: 29rpx;
  font-weight: 600;
}

.md-li {
  display: flex;
  margin-top: 8rpx;
}

.md-li-marker {
  flex-shrink: 0;
  margin-right: 10rpx;
}

.md-li-body {
  flex: 1;
}

.md-bold {
  font-weight: 600;
}

/* ===== 红线内联卡（历史回看时呈现） ===== */
.redflag-card {
  margin: var(--zy-space-sm) 0;
  padding: 20rpx var(--zy-space-md);
  border-radius: var(--zy-radius-card);
  background: var(--zy-color-danger);
  color: #fff;
}

.rf-card-title {
  font-size: 26rpx;
  font-weight: 600;
  margin-bottom: 8rpx;
}

.rf-card-body {
  font-size: 26rpx;
  line-height: 1.6;
}

/* ===== 免责声明常驻细条 ===== */
.disclaimer-bar {
  background: rgba(245, 245, 245, 0.9);
  color: var(--zy-text-weak);
  font-size: 20rpx;
  text-align: center;
  padding: var(--zy-space-xs) var(--zy-space-sm);
}

/* ===== 输入栏 ===== */
.composer {
  background: #fff;
  border-top: 1rpx solid var(--zy-color-border);
  padding: var(--zy-space-sm) 20rpx var(--zy-space-md);
}

.c-row1 {
  display: flex;
  align-items: center;
  gap: 14rpx;
}

.c-input {
  flex: 1;
  background: var(--zy-color-fill);
  border-radius: var(--zy-radius-lg);
  padding: 18rpx 28rpx;
  font-size: 28rpx;
}

.c-row2 {
  display: flex;
  align-items: center;
  gap: var(--zy-space-sm);
  margin-top: var(--zy-space-sm);
}

.cpill {
  border: 1rpx solid var(--zy-color-border);
  border-radius: var(--zy-radius-lg);
  padding: 10rpx 22rpx;
  font-size: 24rpx;
  color: var(--zy-text-title);
  background: #fff;
  transition: transform var(--zy-motion-fast) var(--zy-ease-out);
}

.cpill:active {
  transform: scale(0.98);
}

.cpill.gear {
  color: var(--zy-color-primary);
  border-color: var(--zy-color-primary);
}

.send-btn {
  margin-left: auto;
  background: var(--zy-color-primary);
  color: #fff;
  border-radius: var(--zy-radius-lg);
  padding: 12rpx 36rpx;
  font-size: 26rpx;
  transition: transform var(--zy-motion-fast) var(--zy-ease-out);
}

.send-btn:active {
  transform: scale(0.98);
}

.send-btn.send-disabled {
  opacity: 0.45;
}

/* 工具进度状态条（票 24）：瞬态，输入框上方；tool_start 显示 loading，error 显示失败 */
.tool-progress {
  display: flex;
  align-items: center;
  gap: 12rpx;
  padding: 14rpx 28rpx;
  margin: 0 24rpx 12rpx;
  background: #f0f5ff;
  border-radius: var(--zy-radius-sm);
  font-size: 24rpx;
  color: var(--zy-text-secondary);
}

.tool-progress-spinner {
  width: 24rpx;
  height: 24rpx;
  border: 4rpx solid #d6e4ff;
  border-top-color: var(--zy-color-primary);
  border-radius: 50%;
  animation: zy-tool-spin 0.8s linear infinite;
}

@keyframes zy-tool-spin {
  to {
    transform: rotate(360deg);
  }
}

.tool-progress-error {
  background: var(--zy-color-danger-soft);
  color: var(--zy-color-danger);
}

/* ===== 票 45：语音双向 UI ===== */
/* 按住说话按钮：输入框左侧话筒，长按录音、松开识别（asrEnabled=false 时不渲染） */
.voice-btn {
  flex: 0 0 auto;
  width: 72rpx;
  height: 72rpx;
  border-radius: 50%;
  background: var(--zy-color-primary);
  color: #fff;
  font-size: 32rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.voice-btn-active {
  background: var(--zy-color-danger);
  transform: scale(1.08);
}

.voice-btn-disabled {
  opacity: 0.45;
}

/* TTS 播报按钮：AI 气泡底部，按需点击触发（不自动播放） */
.tts-btn {
  margin-top: 12rpx;
  align-self: flex-start;
  padding: 8rpx 22rpx;
  border-radius: var(--zy-radius-card);
  background: #f0f5ff;
  color: var(--zy-color-primary);
  font-size: 22rpx;
  transition: transform var(--zy-motion-fast) var(--zy-ease-out);
}

.tts-btn:active {
  transform: scale(0.98);
}

.tts-btn-loading {
  opacity: 0.6;
}

.tts-btn-playing {
  background: var(--zy-color-primary);
  color: #fff;
}

/* 智能导诊引导卡（票 62）：纯客户端 UI，风格对齐 department-slots-card 白卡 */
.guide-card {
  margin: var(--zy-space-sm) 0;
  padding: var(--zy-space-md);
  border-radius: var(--zy-radius-card);
  background: #fff;
  box-shadow: var(--zy-shadow-card);
}

.guide-head {
  display: flex;
  align-items: center;
  gap: var(--zy-space-sm);
}

.guide-icon {
  width: 56rpx;
  height: 56rpx;
  border-radius: 50%;
  background: var(--zy-color-primary-soft);
  color: var(--zy-color-primary);
  font-size: 30rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.guide-title {
  color: var(--zy-text-title);
  font-size: 32rpx;
  font-weight: 700;
}

.guide-subtitle {
  margin-top: var(--zy-space-xs);
  color: var(--zy-text-secondary);
  font-size: 24rpx;
}

.guide-steps {
  margin-top: var(--zy-space-sm);
  padding: var(--zy-space-xs) var(--zy-space-sm);
  border-radius: var(--zy-radius-sm);
  background: var(--zy-color-fill);
  display: flex;
  justify-content: space-between;
}

.guide-step {
  color: var(--zy-text-secondary);
  font-size: 22rpx;
}

.guide-chips {
  margin-top: var(--zy-space-sm);
  display: flex;
  flex-wrap: wrap;
  gap: var(--zy-space-sm);
}

.guide-chip {
  padding: 10rpx 26rpx;
  border-radius: 999rpx;
  border: 1rpx solid var(--zy-color-primary);
  color: var(--zy-color-primary);
  font-size: 24rpx;
  transition: transform var(--zy-motion-fast) var(--zy-ease-out);
}

.guide-chip:active {
  transform: scale(0.98);
}

/* 票 65：ambiguous 科室选择卡，视觉与引导卡同族 */
.dep-options-card {
  margin: var(--zy-space-sm) 0;
  padding: var(--zy-space-md);
  border-radius: var(--zy-radius-card);
  background: #fff;
  box-shadow: var(--zy-shadow-card);
}

.dep-options-hint {
  font-size: 26rpx;
  color: var(--zy-text-title);
}

.dep-options-chips {
  margin-top: var(--zy-space-sm);
  display: flex;
  flex-wrap: wrap;
  gap: var(--zy-space-sm);
}

.dep-option-chip {
  padding: 10rpx 26rpx;
  border-radius: 999rpx;
  border: 1rpx solid var(--zy-color-primary);
  color: var(--zy-color-primary);
  font-size: 24rpx;
  transition: transform var(--zy-motion-fast) var(--zy-ease-out);
}

.dep-option-chip:active {
  transform: scale(0.98);
}
