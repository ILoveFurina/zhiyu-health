# 42 - 小程序首页功能目录、tabBar 与排版统一

**What to build:** 按 CONTEXT.md"功能目录"词条落地 C 端新信息架构：tabBar 三 tab（首页 / AI对话 / 我的）；首页 = 问候头 + 当前激活健康档案 chip（可切换/创建）+ 两组功能宫格 + 待办横卡；挂号与报告解读获得脱离 Agent 卡片的独立入口；建立全局设计 token 并统一新旧页面排版。视觉延续现有薄荷绿渐变（`app.acss`），不改品牌方向。

**Blocked by:** 41 - C 端预约挂号浏览/直接挂号与报告解读历史 API

**Status:** ready-for-agent

- [ ] `app.acss` 建立设计 token：主行动色（医疗绿）、三档文字色（标题/次要/弱提示）、统一圆角与间距阶梯；旧页（chat/appointments/health/prescriptions/messages）颜色、圆角、间距硬编码机械替换为 token，chat 页不改交互逻辑
- [ ] `app.json` 配置 tabBar（首页 / AI对话 / 我的，含图标资源）；首页为入口页
- [ ] 新增 `pages/home/`：问候头 + 健康档案 chip（未创建档案时显示引导卡片，规则同 AI 页）+ 宫格（就医服务：智能导诊、预约挂号、报告解读；健康管理：健康档案、我的挂号、电子处方、药品订单）+ 待办横卡（即将就诊的挂号单、待支付药品订单；待支付挂号费待票 35/36 完成后补充）
- [ ] 新增预约挂号多页流：医院列表 → 医院详情（科室）→ 医生列表 → 医生排班 → 确认挂号，逐级 `navigateTo`，页面栈深度 ≤5；确认页调票 41 的直接挂号接口
- [ ] 新增报告解读入口页：上传报告（拍照/相册）+ 历史解读记录列表（票 41 的 GET 接口）；上传后进入关联会话，解读过程与结构化卡片仍在会话流中呈现（对齐支付宝 AQ 模式，CONTEXT.md"报告解读"定义不变）
- [ ] 新增 `pages/profile/`（我的）：账号信息头 + 健康档案管理 + 记录列表（挂号单/电子处方/药品订单/报告解读记录）+ 消息中心（现有 messages 页）
- [ ] 所有 AI 产出区域带"仅供参考，不替代医生诊断"（全局 `ai-disclaimer` 组件），界面无例外
- [ ] 页面使用 `index.{js,axml,acss,json}`，禁止 `.wxml`/`.wxss`/`wx.*`
- [ ] 支付宝开发者工具实测：无控制台错误，人工走通"登录 → 首页宫格 → 挂号流下单 → 报告解读上传 → 我的"
