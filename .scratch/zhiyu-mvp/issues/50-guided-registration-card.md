# 50 - 智能导诊标准科室与科室号源卡

**What to build:** 在票 49 的动态服务城市、标准科室接口和可复用号源卡组件上接入 Agent：用户明确科室时直接查询，症状不明确时多轮追问，形成单一明确标准科室后自动调用 server-java 只读号源能力，并在简短 AI 说明后展示未来 14 天的跨医院科室号源卡。不得再用“需要我帮你查附近医院或有号医生吗”等无法保证执行的话术。

**Blocked by:** 18 - 红线症状（已完成）；31 - 对话主干双栈化（已完成）；49 - AI挂号助手动态城市与确定性自助挂号

**Status:** ready-for-agent

- [ ] 以 `contracts/` 定义标准科室标识及科室号源卡/SSE 契约；Agent 输出标准科室，不用医院实际科室名称做跨医院字符串匹配
- [ ] server-py 新增“按标准科室查询当前服务城市号源”的只读业务工具，位置从受控 `AgentContext` 读取，不作为模型工具入参；移除 `find_hospitals` 作为 Agent 能力及其主动邀约提示词
- [ ] 触发规则：用户明确“科室 + 挂号”时直接调用；多轮导诊得到单一明确科室后自动调用；仍有多个可能科室时先让用户选择，不调用号源工具
- [ ] 科室号源卡前保留简短 AI 说明，只能引用 server-java 返回的统计值；不得编造医院、距离、医生、余号或现实世界供给
- [ ] 无论有号与否都复用票 49 的同一种科室号源卡：有号医生优先，无号医生保留并禁用预约，不替换成单独空结果卡，不跨城市兜底
- [ ] 红线症状由 server-java 确定性规则优先中断，命中时不调用普通号源工具或展示挂号卡
- [ ] AI 说明与科室号源卡均带“仅供参考，不替代医生诊断”
- [ ] server-py TestClient/fake 覆盖“明确科室直查”“多轮后查”“多科室先选择”“工具失败”“无号仍出卡”及工具调用顺序；server-java MockMvc 覆盖回调鉴权与当前患者/城市边界
- [ ] 支付宝开发者工具实测“看看皮肤科挂号 → 说明 + 卡片”和“头疼发烧 → 多轮追问 → 科室建议 + 卡片”，无控制台错误
- [ ] 更新票单 checklist；置 `done` 前在 `README.md` 将 T50 标记为 `[x]50`

## Comments

- 2026-08-06：由 `grill-with-docs` 会话确认。AI挂号助手主卡是入口卡；本票科室号源卡是标准科室确定后的 Agent 业务数据出口，两者不得混用。
- Agent 只确定标准科室；城市、医院、院区、医生、距离、排班、余号与挂号仍由 server-java 确定性处理。
