# Python
__pycache__/
*.pyc
.venv/
venv/
.uv-cache/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.import_linter_cache/

# Node / 前端
node_modules/
dist/
.npm-cache/

# 密钥与环境（所有 .env 变体不入库，仅保留示例）
.env*
!.env.example
*.local

# 云部署渲染产物：compose.oneclick.yaml 由 .env 填密生成，含真实凭据，不入库
/compose.oneclick.yaml
# 云应用层部署凭据（deploy/app/env.cloud.example 为模板）
deploy/app/env.cloud

# 知识库向量 seed：由离线 embedding 工具连真实方舟产出，含向量不入库
# （文本 seed 在 seed.sql 中；向量缺失时运行时检索降级走裸 LLM）
server-java/src/main/resources/seed-knowledge.sql

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# AI 工具 / 小程序构建产物
.kimi-code/
.claude/
.zcode/
miniprogram_npm/
/miniprogram/.mini-ide/

# 微信开发者工具本地配置
/project.config.json
/project.private.config.json
/miniprogram/project.private.config.json
.worktrees/
.scratch/*.log
.scratch/reword/
# 本地开发产物：证书与私钥、pid、WSS 覆盖配置与诊断探针（票单 issues 保持跟踪，不可整目录忽略）
.scratch/*.key
.scratch/*.pfx
.scratch/*.pem
.scratch/*.crt
.scratch/*.cer
.scratch/*.p12
.scratch/*.pid
.scratch/application-local-wss.yml
.scratch/wss-probe.py
# cpolar 内网穿透客户端与本地配置（含 authtoken，不入库）
.scratch/cpolar/
# 彩排录屏（票 26）：带旁白音轨的视频，体积大且非源码，只登记元信息到 rehearsal.md
.scratch/recordings/
# GUI 测试截图（调试产物，不入库）
admin/gui-test-screenshots/
/gui-test-screenshots/
# 已完成票的验收/诊断一次性产物（t60/perf-vision/diag 脚本与输出、playwright 日志；需要复验时按票文档重建）
.playwright-mcp/
.scratch/diag-*.png
.scratch/diag-db-startup-reset.py
.scratch/exec-*.png
.scratch/perf-vision-*
.scratch/t60-*.cjs
.scratch/t66-playbook.md
.scratch/verify-report-picker.cjs
# 临时诊断探针：直连 server-py 拿真实错误（读取 .env token，不入库）
scripts/_diag_chat.py
