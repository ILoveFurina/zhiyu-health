# 智愈（zhiyu-health）

AI 驱动的全链路医疗健康平台：C 端支付宝原生小程序（医疗 AI Agent）+ B 端 React 管理后台，后端双栈：Java 业务后端（server-java）+ Python Agent 层（server-py）（ADR-0009）。两周 demo 项目，评审交付。

## Language

**C端**:
面向患者的支付宝原生小程序，承载医疗 AI Agent，是用户的"第一联系人"。
_Avoid_: 患者端、用户端、小程序端

**B端**:
面向医生与医院运营者的管理后台，为 C 端 Agent 提供数据支撑与服务能力。
_Avoid_: 管理端、运营后台、医生端

**业务后端**:
Java 服务（`server-java/`），平台唯一对外入口与唯一业务写入方：鉴权、审计、规则引擎、号源扣减、B 端全部 API 与 C 端业务 API。对话请求也由它流式转发给 Agent 层。
_Avoid_: "后端"（双栈语境下必须指明哪端）

**Agent 层**:
Python 服务（`server-py/`），承载 LangGraph 对话编排、LLM/语音对接与医学知识检索（Neo4j/pgvector 直连只读），无业务写入权，业务数据一律经工具回调业务后端。
_Avoid_: AI 后端、Agent 后端

**统一入口**:
端侧一切请求（含 C 端对话）都先进业务后端的架构约定：鉴权、审计、限流只此一处；对话由业务后端 SSE 流式调 Agent 层，token 逐跳透传。
_Avoid_: 直连 Agent 层

**工具回调**:
Agent 层的业务工具经 HTTP 调用业务后端读写业务数据的链路。知识检索（Neo4j/pgvector）不属于工具回调，由 Agent 层直连只读。
_Avoid_: 反向调用、内部 API（泛指时）

**智能导诊**:
C 端对话中的症状导诊过程：症状描述 → 必要追问 → 科室建议。入口只在当前会话插入一张本地引导卡，不调用 Agent、不创建新会话，也不清空既有上下文。用户明确表达科室挂号意图，或多轮导诊已得出单一明确标准科室时，系统自动查询 server-java 号源；先由代码根据返回数据确定性生成最早可约时间和有号医生摘要，再展示科室号源卡，不让 LLM 自由生成号源事实。科室仍不明确时才继续追问或让用户选择，不得空口询问是否需要推荐，也不得编造医院、医生、地址、距离或余号。
_Avoid_: 导诊机器人、智能问诊（问诊是医生行为，Agent 只做导诊与预问诊）

**AI挂号助手**:
C 端挂号能力的产品名称，含两条路径：无症状或科室信息时走确定性的自助挂号目录；用户提供科室或症状信息时由 Agent 理解或推断科室，再调用 server-java 只读号源工具展示科室号源卡。医院目录、医生、排班、余号与最终挂号均为确定性业务能力。
_Avoid_: AI挂号、Agent 挂号（会误解为目录查询和挂号由模型自动执行）

**预问诊**:
提交在线问诊单前由 Agent 与患者沟通并整理主诉、现病史与过敏史的过程；进入时锁定当前健康档案作为本次服务对象，问诊完成前不得因用户切换档案而改变，它只为医生准备病情摘要，不包含医生接诊或诊断行为。
_Avoid_: 在线问诊、智能问诊（前者必须有医生参与，后者会误解为 Agent 在行医）

**预问诊模式**:
患者从明确的在线问诊入口进入的专用引导状态，以可见进度组织 Agent 的自然语言追问，并持续说明尚未连接医生；普通会话中的症状描述不会自动进入该模式或创建在线问诊单。
_Avoid_: 普通会话、在线问诊中、按意图静默切换

**在线问诊**:
患者无需到院、由医生通过图文或视频完成的远程医疗服务，与预约线下门诊号源的挂号相互独立；当前 demo 不收在线问诊费、不创建问诊收费记录，药品订单仍独立模拟支付。Agent 独自追问只属于预问诊，不算在线问诊。
_Avoid_: AI问诊、Agent 问诊、把预问诊对话称为在线问诊

**在线问诊单**:
患者确认病情摘要后形成的非定向业务记录，先进入建议科室待接诊池，待医生接受时才绑定该医生；等待期间患者可取消，默认 10 分钟无人接受则失效，取消或失效后可复用原摘要重新提交。每份健康档案最多存在一条等待或进行中的在线问诊，重复提交幂等返回原单；它不占用线下号源，也不等同于挂号单。
_Avoid_: 挂号单、在线挂号单

**科室待接诊池**:
按预问诊建议标准科室在平台范围内路由在线问诊单的医生工作队列，不受挂号服务城市、医院或线下号源限制；拥有 B 端医生账号且实际科室映射到该标准科室的医生均可查看，患者不指定医生，首位接受问诊的医生与问诊单完成绑定，不另设在线开关或在线排班。
_Avoid_: 自动指定医生、随机分配医生、线下号源池

**在线问诊进度**:
C 端与 B 端共同推进的一条端到端进度，固定为“AI 预问诊 → 患者确认摘要 → 等待医生接诊 → 医生问诊中 → 问诊已完成”；只有医生在 B 端明确接受问诊才能从等待推进到问诊中，查看摘要不推进状态，预问诊内部的信息采集情况不另设用户可见的子进度。
_Avoid_: 预问诊完成度、聊天轮数进度、医生查看进度

**在线问诊交流**:
医生接受问诊后与患者进行的独立交流，由 server-java 持久化并经 C/B 端轮询：图文承载文字与图片两类消息——患者可发送文字与图片（图片经 MinIO 旁路持久化，医患双方按 object_key 回看），医生以文字回复并只读查看图片；语音输入只经 ASR 落成文字，问诊记录不留语音（见"语音输入"）。等待接诊期与问诊完成后禁止发送，完成后只读；视频只模拟连接界面与状态而不传输真实音视频；交流不得与患者和 Agent 的会话消息混存。
_Avoid_: Agent 会话、实时 IM、真实视频通话、语音消息

**语音输入**:
C 端按住说话、经 ASR 识别为文字后回填输入框的输入方式，识别结果可编辑再发送；语音本身不是一种消息类型，AI 对话与在线问诊交流均只落文字（ADR-0020），通道与开关（contracts/voice.json asr_enabled）双端共用。
_Avoid_: 语音消息（录音作为独立消息发送）、语音识别（识别只是 ASR 一步，语音输入是含录制/识别/回填的完整输入方式）

**就医位置**:
用户在本次小程序使用期间确认、用于同城医院筛选与距离计算的位置；用户可跳过或重新校正。Agent 只通过受控上下文让业务工具使用该位置，不把精确坐标当作对话内容或健康档案长期保存。
_Avoid_: 患者住址、常住地址、Agent 定位

**服务城市**:
由用户确认的就医位置与平台院区数据共同决定的当前挂号城市，是医院和号源查询的硬边界。城市不是前端或 Agent 写死的常量；演示 seed 当前只提供郑州市数据，B 端增加其他城市院区后即可形成新的服务城市。
_Avoid_: 默认城市、固定城市

**找医院**:
用户主动进入后，基于本次小程序使用期间已确认的就医位置查询就诊地点的确定性业务目录能力，不经过 Agent。小程序启动时可让用户确认或跳过定位，智能导诊不得再用对话话术索取位置；当前城市是硬边界，只展示该城市的平台医院并在城市内按距离排序，无结果时展示空状态，不跨城市推荐远距离医院。
_Avoid_: 医院推荐（带有 AI 或医学判断含义）、导诊后自动荐院、附近医院邀约

**院区**:
同一家医院下具有独立地址和地理坐标的就诊地点，科室归属具体院区。用户选择医院后先选择院区，再沿确定性的科室、医生、排班链路完成挂号。
_Avoid_: 分院（除非医院正式名称如此）、把院区当成独立医院

**科室分类**:
一家医院为组织和展示本院科室而维护的分类及顺序，各院区共享该医院的分类体系但可分别启用具体科室。分类不是平台统一枚举，也不参与 Agent 的医学判断。
_Avoid_: 平台科室枚举、医学知识图谱科室（后者是通用医学知识节点）

**标准科室目录**:
平台维护的跨医院科室导航，按“科类 → 标准科室”组织，供城市级“更多科室”、搜索和 Agent 科室建议使用。医院自由维护的实际科室必须映射到一个标准科室，跨医院号源查询只通过该映射匹配，不依赖名称字符串相等。
_Avoid_: 医院科室分类、院内科室目录

**科室号源卡**:
标准科室确定后展示的跨医院挂号卡，统一呈现当前城市未来 14 天的日期状态及匹配医生；无论是否有可约号源都返回同一种卡片，有号医生优先，无号医生保留展示并禁用预约。卡片中的医院、距离、医生、排班与余号全部来自 server-java；医生条确定性展示职称与擅长作为推荐理由（票 60），平台不提供评分、好评率等评价数据。
_Avoid_: 无号空结果页、AI 生成号源、医生评分/好评率

**推理档位**:
C 端控制 Agent 响应速度与推理深度的选择，包括快速回答、自动和深度思考；自动档按业务场景选择，普通对话优先速度，复杂解读优先质量。
_Avoid_: 模型档位、思考等级

**通用药品知识解释**:
C 端 Agent 对药品用途、说明书与常见注意事项的非个性化说明；不结合健康档案决定具体药品、剂量、替代药或某位用户是否可以服用。拍药盒（票 51）即该形态：vision OCR 提名药名后经 realtime 通道流式输出通用说明书，用户可见语义为「一段简明摘要 + 2～4 个按药品特点选择的章节 + 末尾安全提示」，流尾引导咨询医生或药师。
_Avoid_: 用药建议、个性化用药指导

**病情摘要**:
问诊前由 Agent 整理患者主诉形成的结构化摘要，供医生接诊台查看。
_Avoid_: 病历、诊断报告（Agent 不产出病历，产物一律叫"病情摘要"）

**会话**:
患者与 Agent 的一段连续多轮对话，归属患者账号（不随健康档案切换），含标题与消息列表。首条消息发出时才创建（惰性创建），标题取首条用户消息截断。新对话只清空对话上下文，健康档案始终可读。领域层与接口一律用"会话"。
归属层级：会话挂在患者账号（登录身份）上，**不**绑定任何一份健康档案——无档案也能正常对话。会话列表对当前登录身份全部可见，切换档案不过滤。而每轮对话里依赖档案的能力（报告解读个性化）只读"当前激活档案"，用药禁忌检查只在 B 端开方流程读取档案、不在 C 端对话轮次内，均不把会话与档案绑定。
_Avoid_: 聊天、线程、session（代码标识符除外）

**对话轮次**:
会话内一次用户请求及其对应处理结果组成的生命周期单元；一经接受便独立于任何实时连接持续到完成或失败。
_Avoid_: 请求任务、一次聊天

**对话记录**:
C 端抽屉中按最近活跃倒序排列的历史会话列表（上限 50 条），支持新对话、切换回看续聊、单条删除。删除会话只删对话数据，不影响挂号单等业务实体。为 UI 文案专用词。
_Avoid_: 聊天记录、历史消息

**号源**:
某医生某排班时段内可预约的号；挂号即扣减号源，必须防超卖。
_Avoid_: 名额、库存

**健康档案**:
患者账号下的一份个人健康数据集合：基础信息、过敏史，以及挂号单、电子处方、报告解读记录的时间线；同一账号可为本人或家人创建并切换多份档案。未创建档案时 AI 页显示引导卡片。
_Avoid_: 病历夹、健康记录

**报告解读**:
患者上传检查报告后形成的独立 AI 解读记录，可关联发起它的会话；上传过程与结构化解读卡片在会话中呈现，但报告解读不等同于普通消息。报告原图采用"即用即弃"模型：multipart 上传后仅在 `ReportUploadStagingService` 进程内内存暂存 5 分钟，解读完成即移除，不落盘、不入库、不进对象存储，只留 `result_json` 与 `file_name`。隐私敏感度高的医疗文书不留原图，与拍照分析、在线问诊交流图片的持久化模型并存，由场景语义决定。
_Avoid_: 报告诊断、检查结论

**拍照分析**:
患者拍摄皮肤/饮食/舌苔等生活影像，经视觉管道分析后产出结构化建议卡片的流程（票 15/16/17）。与报告解读的关键区别在于**图片是一等公民**：原图持久存于 MinIO 对象存储，PostgreSQL 只存路径，`messages` 表以 `image` kind 承载图片路径，用户可在历史会话中回看原照。MinIO 是**旁路持久化**--只为"历史会话回看原照"服务，不介入视觉分析热路径：server-java 收到 multipart 上传后照旧把字节流透传给 server-py 分析（`AgentClient.interpretVision` 取图链路不变），同时写一份进 MinIO，server-py 零改动、不新增 MinIO 客户端依赖。分析结果卡片（`skin_analysis`/`diet_analysis`/`tongue_analysis`）作为独立 AI 消息回落会话，与图片消息分离--图片是"输入留存"，卡片是"AI 产出"，两者解耦。照片是用户自有生活影像，隐私敏感度低于医疗报告，故持久化；报告解读沿用即用即弃，两种模型不强行统一。
_Avoid_: 图像识别（识别只是视觉管道的一步，拍照分析是含上传/留存/分析/回落的完整流程）

**中医辨证**:
拍舌苔场景（票 17）引入的中医体质辨识与调理建议，是拍照分析中合规负担最重的子场景。三条合规边界（ADR-0024）：① 调理建议**只讲方向**（作息/运动/饮食原则/通用食材如山药红枣），不出现具体药材/方剂/剂量--严格落在硬约束 2 与 ADR-0016"通用知识解释"白名单内，不触碰"个性化用药决策"；② 舌诊卡片叠加**中医专属免责**"体质辨识仅供参考，不替代中医面诊"（`contracts/disclaimer.json` 新增，与通用免责并列），因"面诊"是通用文案"不替代医生诊断"覆盖不到的中医特定语义；③ 舌象提示急症只做**软兜底**（prompt 引导 LLM 输出"建议尽快就医"），不回流到 `RedFlagRuleEngine` 确定性规则通道--红线引擎维持只接受用户文本的现状。体质辨证本身是中医诊断行为，故比 15 拍皮肤/16 拍饮食更严，但调理不出药材使其仍属"通用知识"而非"个性化用药"。
_Avoid_: 体质分析、舌诊诊断（辨证是中医诊断行为，但本平台只做体质辨识参考 + 调理方向，不做诊断结论；"诊断"一词易误导）

**药品查询**:
拍药盒（票 51 收口后语义）的完整流程：vision 管道只负责识别候选药名（OCR 提名，不做药品分析），客户端拿到药名后经 realtime 通道发送 `medication_name` 信封，由 LLM 按通用语料流式输出说明书，轮次落 `KIND_TEXT` 消息，流尾由 server-java 出口兜底注入免责声明并追加"具体是否适用请咨询医生或药师"（ADR-0028）。说明书用户可见语义为半结构化排版（票 53）：开头一段简明摘要、中间 2～4 个按药品特点选择的章节、末尾安全提示。说明书不查 `medications` 业务表、不读健康档案、不做个性化禁忌判定——禁忌能力仅在 B 端医生开方流程（`ContraindicationService` + 规则引擎）。票 14 的双出口卡片（`medication_info`/`medication_safety`）与「查药品」文字入口已删除，文字版能力经 `medication_name` 信封保留（拍药盒识别后自动携带）。
_Avoid_: 药品识别（识别只是 vision 提药名一步，药品查询是含提名/说明书流/流尾兜底的完整流程）、用药安全判定（C 端不做，仅 B 端开方）

**医学知识图谱**:
存于 Neo4j 的医学知识网络：症状、疾病、科室、药品、禁忌五类节点及关系边。只装知识，不装业务实体（医生/号源在 PostgreSQL）。供导诊 Agent 推理（`traverse_graph` 一跳扩展）、禁忌检测（server-java 确定性规则）与 B 端图谱可视化使用。疾病节点承接"症状->疾病->科室"推理链，是图谱区别于知识库（pgvector 文本块）的核心结构。
_Avoid_: 知识库（"知识库"特指 pgvector 中供向量检索的文本块）

**知识库**:
存于 PostgreSQL `knowledge_chunks` 表、经 doubao-embedding-vision 向量化的症状场景知识文本块集合。表 schema 由统一 schema.sql 管理；embedding 离线生成属数据准备（server-py），运行时 server-py 对该表只读检索。供导诊回答接地（见 ADR-0010）。
_Avoid_: 知识图谱（"知识图谱"特指 Neo4j 中的图结构医学知识网络）

**知识源选择器**:
导诊回答的接地策略选择：RAG（pgvector 知识库检索）或知识图谱（Neo4j `traverse_graph` 一跳扩展）。rag 与 graph 互斥（同一请求只注入一个知识工具），加裸 LLM 共三态。检索失败或空召回时自动降级走裸 LLM。每请求字段 `knowledge_source` 经 `contracts/` 定义、业务后端透传至 Agent 层；B 端现场切换出口归票 25。
_Avoid_: RAG 开关（实际是知识源选择器，不只是布尔开关）

**降级**:
知识源选择器选了 RAG 或图谱但检索失败、空召回或该源未实现时，静默改走裸 LLM 生成回复的兜底机制。降级状态经 SSE `knowledge` 元事件暴露（ok/degraded/unavailable），用户侧不强提示，演示与 B 端 trace 可见。
_Avoid_: 回退、降级策略

**排班**:
医生在某日期某时段出诊的安排，号源（总数/剩余）挂在其上。
_Avoid_: 出诊计划、班次

**挂号单**:
患者对某排班的一次预约记录，含分配序号与状态（已约 / 已取消 / 已接诊）。
_Avoid_: 订单、预约单

**电子处方**:
医生在线下接诊或在线问诊中开具的药品清单（药品 + 用法用量），复用同一套审核状态流转；问诊完成不等待审核。处方全程对 C 端可见（PENDING 为审核中、REJECTED 为未通过），但用药解读只随 APPROVED 出现，且只有 APPROVED 可发起购药（见"药品订单"）（票 60）；驳回即终态——患者被引导重新发起问诊或挂号，不改方重提（ADR-0030）。处方经 appointment_id 或 online_consultation_id 两个真实外键二选一关联来源（恰好一个非空，各保持一对一唯一），来源类型只是外键派生展示值，数据库不落 source_type 列。
_Avoid_: 药方、处方单

**接诊记录**:
医生完成一次线下接诊或在线问诊时落库的诊断结论与医嘱，与电子处方同样按 appointment_id / online_consultation_id 双外键二选一关联来源；它是处方审核队列诊断/医嘱上下文的读取来源，禁止用无外键保障的 source_type/source_id 多态引用替代。
_Avoid_: 病历、就诊记录

**临床上下文**:
server-java 统一解析一次可开方临床场景的 interface：从已鉴权的线下挂号单或在线问诊单派生患者、锁定健康档案、接诊医生、来源与发生时间；开方、禁忌检查、服药提醒等调用方一律经它派生，不各写一套来源分支，患者与医生身份绝不接受请求体传入。
_Avoid_: 上下文对象、问诊上下文

**免责声明标注**:
所有 AI 产出内容（导诊建议、病情摘要、处方/报告解读、拍照分析、就诊小结、用药解释与替代建议等）必须携带的固定文案"仅供参考，不替代医生诊断"。选题硬约束，任何界面无例外。
_Avoid_: 提示语、小字

**就医安全提醒**:
C 端 Agent 在正常对话中针对可能需要紧急就医的描述给出的文字建议，与其他回复使用相同的消息形态，不触发弹窗、特殊卡片或流程中断，也不阻止用户继续对话或查看号源。
_Avoid_: 红线症状、紧急提醒、急症拦截（C 端语境）

**药品订单**:
患者基于已审核（APPROVED）电子处方发起的购药订单，订单明细复用处方明细（药品 + 数量 + 单价）。状态流转 `UNPAID -> PAID -> DONE`，未支付订单可 `CANCELLED`；支付动作为模拟状态机（ADR-0012）。库存为预扣模型：下单时扣减 medications.stock，取消时回补，与号源同构禁止先查后改。履约方为平台自营药房（见"平台自营药房"），订单无医院/药店维度。
_Avoid_: 购药单、药单、药方订单

**平台自营药房**:
药品购药的唯一履约方：全平台一份药品目录与库存（medications 全局，不挂医院、不设外部药店），APPROVED 处方发起的药品订单由其统一履约。B 端药品/库存/订单管理即平台运营方对自营药房的管理；药师职责并入 admin。题目建议的多药店推荐与"与药店数据打通"（处方外流模式）明确不实现（ADR-0026）。B 端药品管理页的"同步药店库存"为 Mock 演示展示层（`/api/b/demo/**`，虚构药店静态快照，不触碰业务库存），不属于本词条的业务履约语义。
_Avoid_: 医院药房、药店、药店推荐、处方外流

**挂号收费**:
患者挂号时产生的诊查费，金额由医生职称决定（doctors 表 registration_fee，按主任医师/副主任医师/主治医师等定价）。挂号即产生 UNPAID 收费记录（payments 表，关联 appointment_id），不阻塞号源扣减；状态流转 `UNPAID -> PAID`，支付动作为模拟状态机（ADR-0012）。payments 表只承载挂号收费，药品订单的支付状态内嵌在订单状态机中，两者解耦。
_Avoid_: 挂号费订单、预约费

**工具进度事件**:
Agent 调用可视化（票 24）的运行时载体：每次工具调用产生两条 SSE 事件--`tool_start`（工具名 + 参数摘要，下发时 C 端显示"正在…"）与 `tool_end`（结果枚举 success/error/skipped + 耗时 duration_ms，由 server-java 按墙钟计算）。两态对齐 LangGraph `agent_actions` 流的"发起/返回"两个天然时刻，不插值、不造中间心跳；长工具（vision 150s+）的无反馈问题由 vision 自身结构校验分片流解决，不污染通用 trace 契约。事件名、阶段与结果枚举的唯一事实源是 `contracts/sse-events.json`（票 24 在该文件新增 `trace_events` 字段，与有序的 `stream_events` 并列；trace 事件可穿插、无固定顺序）。trace 事件名集合必须与 `card_events`/`ai_card_kinds` 严格不相交，且不得与 `done` 重名，由 `ContractsConsistencyTest` 钉死。trace 事件落 `agent_call_logs` 必须走独立可失败路径，不得复用 `persistEvent` 同步事务--写入失败只产可观测错误，不连坐主对话流（票 24 硬约束）。
_Avoid_: 工具日志、tool trace（"trace"指整条调用链，单条事件叫"工具进度事件"）

**Agent 调用日志**:
B 端"Agent 调用日志"页展示的每轮对话工具调用链，持久化于 `agent_call_logs` 表（事件粒度：每条工具进度事件一行，`tool_start`/`tool_end` 用 `tool_call_id` 配对）。字段为白名单：round_id/conversation_id/patient_id/tool_call_id/tool_name/phase/result/duration_ms/error_code/seq/created_at，**无任何原文列**（不存工具参数与返回体，硬约束 5）。仅 `admin` 角色可见，经 `GET /api/b/agent-call-logs/conversations`（会话摘要导航）与 `GET /api/b/agent-call-logs?conversation_id={id}`（扁平事件列表）查询；不存在的会话返回空列表。
_Avoid_: Agent 日志、工具调用记录（"对话记录"指 C 端会话列表，与调用日志不同）

**工具进度状态条**:
C 端对话页输入框上方的瞬态状态条，承载工具进度事件的用户可见反馈。`tool_start` 显示"正在{中文文案}…"（带 loading），`tool_end` 按结果分流：success 短暂显示后清空，error 显示"{工具中文名}失败"，skipped 静默不显示（降级对用户不可见，与"降级"词条一致）。状态条是瞬态运行时状态，不进入 messages 数组（持久对话记录），避免与紧随的卡片消息视觉重复。工具名->中文文案映射在 miniprogram 本地维护。
_Avoid_: 进度气泡、加载气泡（"气泡"指消息流内的持久消息单元）

**功能目录**:
C 端首页的功能宫格：分"就医服务"（智能导诊、预约挂号、报告解读）与"健康管理"（健康档案、我的挂号、电子处方、药品订单）两组，是业务后端能力的图形化入口，与 Agent 卡片入口并存。宫格每个入口指向独立页面，不经对话中转；C 端主导航为 tabBar 三 tab（首页 / AI对话 / 我的）。
_Avoid_: 功能列表、服务大厅

**站内消息通道**:
C 端消息页（`pages/messages`）聚合展示给患者的所有站内通知能力，是 UI/出口层面的概念，不等于某一张表。通道下挂两类物理来源：`in_app_messages` 装一次性事件（就诊指引卡、就诊小结、处方审核结果通知等，append-only，`disclaimer NOT NULL`，按事件来源外键幂等——挂号事件 `UNIQUE(related_appointment_id, type)`，处方审核事件 `UNIQUE(related_prescription_id, type)`，票 60）；`med_checkin_records` 装服药打卡生命周期（PENDING->CHECKED 状态机，`due_date` 到点才在通道可见，打卡后离开通道进时间线）。两者物理分离、各守各的约束，只共用消息页 UI 与免责声明标注。
_Avoid_: 站内消息表、消息中心（"通道"指出口能力，非某张具体表）

**服药打卡**:
基于已审核通过（APPROVED）电子处方的用法用量，由 server-java eager 预生成的到点服药提醒与打卡记录，承载在 `med_checkin_records` 表上。处方审核通过那一刻按 `duration` 解析出的天数展开为每日一条 PENDING 记录（`due_date` 到点才在站内消息通道可见），患者点"已服用"后状态机推进为 CHECKED 并落 `checked_at`，CHECKED 不可回退。连续天数（streak）由 server-java service 按打卡记录现算（漏一天归零），不存派生列。打卡记录直接归属 `health_profile_id`+`patient_id`（report_interpretations 先例），不经 appointment 中转；时间线第 4 分支读 CHECKED 记录。server-py 完全不参与。
_Avoid_: 用药提醒（提醒只是 PENDING 态的别名，打卡才是完整生命周期）、服药记录（容易混成纯日志，实际是带生命周期的状态机记录）

**就诊指引卡**:
挂号成功后随关怀消息下发给患者的结构化就诊提示（地址/楼层/携带材料/注意事项），承载于一条 `in_app_messages`（type=`appointment_care`），由 server-java `AppointmentService.create()` 事务内写入，覆盖 C 端 Agent 与 B 端直接挂号所有入口，靠 `UNIQUE(related_appointment_id, type)` 幂等。内容字段（关怀语/医院/科室/医生/排班/地址/楼层/材料/注意事项）由 `contracts/appointment-care.json` 定义；地址/楼层/材料/注意事项来自 `hospitals` 表静态列，非 LLM 生成。消息页按 type 渲染为卡片，底部带免责声明标注。
_Avoid_: 就诊通知、导诊卡（"导诊"是 Agent 流程，指引卡是挂号后副作用产物）

**随访**:
在线问诊完成后由 server-java eager 生成的一次性关怀消息（如"您好些了吗？药吃完了吗？"），承载于 `in_app_messages` 并以 `visible_at` 延迟可见（默认问诊完成 3 天后，演示可缩短）；文案确定性、不经 Agent、不读病情内容，与服药打卡的周期性提醒互补（票 60）。
_Avoid_: 主动关怀（题目用语，范畴更大）、智能回访（不经 Agent，无"智能"）

**情绪反馈**:
C 端 Agent 回复携带的三档情绪标注（calm/anxious/fearful），驱动 AI 气泡配色与安抚语。由 server-py 在主回复完成后串行非流式 LLM 调用产生（`response_format=json_object`+pydantic 校验，复用视觉管道结构化输出范式），失败降级 calm；枚举、默认值与安抚语映射在 `contracts/emotion.json`。情绪字段挂 `message` 事件（不新增 SSE 事件），持久化于 `messages.emotion` 列供历史回看复现情绪色。情绪反馈与红线症状正交：红线是 server-java 确定性规则，情绪只是 UI 反馈，不触发中断。
_Avoid_: 情感分析、情绪识别（反馈是 UI 驱动标注，不是分析/诊断行为）

**安抚语**:
情绪反馈触发时附于 AI 气泡底部（disclaimer 上方）的确定性安抚文案，从 `contracts/emotion.json` 的 `soothing_texts` 取，不由 LLM 现场生成。calm 无安抚语，anxious/fearful 各一条。与 AI 回复共用同一条免责声明标注，不单独标注、不作为独立消息、不进 messages 数组。
_Avoid_: 安慰话术、心理疏导（医疗场景不做心理干预，仅 UI 安抚）

**演示武器包**:
为两周 demo 评审服务的支撑能力集合，非医疗业务实体：B 端演示看板、演示重置、并发抢号脚本、知识源现场切换出口。四件套物理收口在 `/api/b/demo/**` 前缀下、仅 admin 可用、由环境开关默认关闭；生产/未授权环境一律拒绝。它与业务能力的边界是：演示武器包只消费业务数据（只读聚合）或回到 seed 初始态（重置），不定义新的业务实体或业务流程。
_Avoid_: 演示功能、demo 工具（"武器包"强调它是评审演示的一整套装备，非零散工具）

**演示重置**:
演示前把 PostgreSQL 演示业务表与 Redis 号源计数一键回到幂等 seed 初始态的受保护操作，仅作用于业务数据；pgvector 知识块与 Neo4j 医学知识图谱是版本化只读基线，重置不触碰，只在完成后做数量一致性断言。重置顺序固定为"冻结 C 端入口 -> 清 Redis 演示键 -> 清 PG 演示业务表 -> 重灌幂等 seed -> 重建 Redis 计数 -> 解冻 -> 断言"；中途失败保持冻结、返回可恢复步骤清单，接口幂等可从失败步重跑，不自动回滚。
_Avoid_: 数据重置（泛指，未区分业务数据与知识基线）、清库（重置不清知识基线，"清库"误导）

**演示冻结**:
演示重置进行中对全部 C 端入口（`/api/c/**`）的临时封锁，由 server-java 进程内 `AtomicBoolean`（`DemoFreezeGate`）承载，`DemoFreezeFilter` 在重置期间对 C 端请求返回 503。B 端只读与重置接口本身不冻结，以便演示者从 B 端观察断言结果与重跑。冻结为瞬态：重置完成解冻；中途失败保持冻结直到重跑成功。单实例拓扑下进程内标志即可，重启丢失即解冻属可接受人工步骤。
_Avoid_: 熔断（冻结是演示重置的受控子步骤，非故障保护）

**演示剧本**:
评审演示的逐拍操作手册（`.scratch/zhiyu-mvp/demo-script.md`）：演示前检查单、主线逐拍（总预算 ≤5 分钟）、三条可独立演示的支线亮点、每拍“操作/话术/预期/时长/出错兜底”对照表。随演示范围冻结而冻结，是联排与录屏的统一依据。
_Avoid_: 演示流程、脚本（“脚本”专指 `scripts/` 下的可执行文件）

**彩排记录**:
票 26 联排与录屏的工作记录（`.scratch/zhiyu-mvp/rehearsal.md`），是演示范围动态清单的唯一载体：冻结票单清单及纳入口径、seed 核对表、演示账号清单、每遍联排结果（排错遍/验收遍）、录屏元信息。视频文件本身不入库，只登记元信息。
_Avoid_: 彩排日志、演示报告
